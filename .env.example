# 应用配置
APP_NAME=PLMS
APP_VERSION=1.0.0
APP_PORT=8080
APP_ENV=production

# 数据库配置（云服务器上的数据库）
DB_HOST=your-db-host
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your-db-password
DB_NAME=plms

# JWT 密钥（生产环境请设置强密码）
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# SSL 证书路径（可选，用于非 Docker 部署）
# SSL_CERT_PATH=/path/to/cert.pem
# SSL_KEY_PATH=/path/to/key.pem
